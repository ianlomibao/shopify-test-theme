{{ 'section-button.css' | asset_url | stylesheet_tag }}

<button type="button">Sample text</button>

{% schema %}
{
    "name": "Button",
    "settings": [
        {
            "id": "show_button",
            "type": "checkbox",
            "label": "Show button",
            "default": true
        }
    ],
    "presets": [
        {
            "name": "Button",
            "category": "Button"
        }
    ]
}
{% endschema %}